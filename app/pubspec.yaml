name: mtek_inventory
description: M-Tek Fire & Safety Ltd — Inventory & Business App (one codebase: Android, Windows, Web/PWA).
publish_to: "none"
version: 0.1.0+m1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0          # ₦ currency + date formatting
  fl_chart: ^0.68.0      # Insights & Summary charts

  # Planned for M2/M3 (offline DB, state, backend, documents):
  # drift: ^2.20.0         # local SQLite cache + sync queue
  # flutter_riverpod: ^2.5.1
  # supabase_flutter: ^2.5.0
  # pdf / printing / share_plus — receipts & invoices

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
